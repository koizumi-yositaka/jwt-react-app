import React from 'react'
import { LoginForm } from './LoginForm'

export const Login = () => {
  return (
    <div>
        <div>Login</div>
        <LoginForm mode='login'></LoginForm>

    </div>
  )
}
